- 地図を評したとき、及び画像をダウンロードしたときに、画像の縮尺が小さすぎて描画されている円が表示されない。指定した半径に応じて地図の縮尺の調整が必要。
- 企業一括抽出の際のデータは店舗ごとにファイルを分ける必要がなく、対象企業に絞り込まれたデータが1つのファイルで出力されればOK。可能であればzipではなく、1つのCSVファイル（encodingはcp932）で出力されることが望ましい。
- 企業一括抽出は企業一括データ抽出と企業一括画像抽出に分けたい。
- app上のラジオボタンで機能が切り替えられるにもかかわらず、app,help,map_bulk,map_singleなどの表示もアプリ上に記載があり、紛らわしい。これを削除して、app上のラジオボタンのみで機能が切り替えられるようにしたい。
- マルチページ構成をやめる。ページ上部に企業名称・小売店名称・半径の選択ボタンと企業一括画像ダウンロードボタン・企業一括データダウンロードボタンを配置し、下部に地図、更にその下にデータ出典元を記載する。データ出典元は小さく、目立たない形でOK。企業名称が選択されたら、その企業名称で小売店名称は絞られるようにする。
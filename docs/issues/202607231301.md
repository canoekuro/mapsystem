- データ更新にもう1ファイル、RDPデータが追加になった。同様にアップロードのインターフェースを準備する。格納先 Volume フォルダの指定は他2ファイルと同様。格納されるファイル名称は「rdp」とする。
- 「マップ」ページのsidebarに商談用資料ダウンロードと店舗POPダウンロードのボタンがあるが、結局ユースケースとしては両方DLすることが非常に多いので「商談資料・店舗POPダウンロード」という1つのボタンにして、押したら両方がダウンロードできるようにする。
- 「マップ」ページの名前を「店舗担当用」にする。また、追加で「本部担当用」ページを作る。「本部担当用」ページの機能は「企業G名称」と「半径」を指定してデータ取得をすると、「店舗別 推進園数」の表が出る 、かつ店舗別推進園数がダウンロードできるというもの。よって、sidebarに企業G名称と半径指定とダウンロードボタンを置き、メインページに表を置くようにする。「本部担当用」ページにマップは不要。
- 施設リストに表示される距離が現状小数点以下2桁（例.約0.56km）の表示になっているが、これを小数点以下1桁（例.約0.6km）にする。

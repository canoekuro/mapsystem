- ローデータダウンロードボタンを廃止
- 企業名称、取得半径、都道府県、小売店名称の選択フィルタをsidebarに移動（ページの下、ダウンロードの上）
- データ取得後の取得店舗数の件数表示を廃止
- データ出典の表記を店舗別推進園数の表の下に移動
- マップ上の表示（images/map.png）とpptx上の表示（images/pptx.png）が異なる。具体的には線が点線ではない、推進園を示す円のサイズが異なる。これをマップ上の表示に合わせる。
- 推進園区分ごとの色分けを廃止したが、theme.tomlに以下の表記が残っているため、これを施設の色1種類だけを書き換えるように変更する。「テーマ設定ページ」での色変更も同様。加えて、ここで設定した色をpptx側にも反映する。
```
[theme.facility_colors]           # 推進園区分 -> 色（定義順は凡例の表示順）
"認可保育所"   = "#7030A0"
"認定こども園" = "#F5C518"
"幼稚園"       = "#2EAA6E"
```

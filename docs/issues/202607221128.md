- 1kmなどで取得した場合に、その圏内に推進園のある小売店しか現状は表示されない。（1km圏内に0だった場合はデータとして存在しなくなってしまう。）そのため、小売店のテーブルを別で作り（以下のSQLのイメージ）、データ取得時は小売店テーブルと小売店×推進園テーブルの2つのデータフレームを取得し、フィルタするときは小売店テーブルの値を選択肢として表示するようにする。（それでデータが無い場合はマップを表示せず、0件である旨をアラートに出す。）また、マップ下部の表に関してはカウントの結果を小売店テーブルにleft joinしたうえで、推進園が圏内に0件の場合は推進園数を0とするようにする。

```
SELECT
  Distinct
  `企業名称`, 
  `店舗名称`, 
  `店舗コード`, 
  `店舗_都道府県`
FROM
  kh_aigrd_databricks_prd_01.default.immune_promo_nursery
```
- 画像出力機能を変更し、パワーポイントのプレースホルダーに画像化したマップを張り付けたpptxファイルを作成し、ダウンロードできるようにする。現状はデータダウンロード→都道府県選択→ダウンロードですが、都道府県選択は廃止する。その代わり、ある程度生成に時間がかかってもよいので、サーバ側の負荷も考慮する。また、現状は「画像ダウンロード」になっているが、「商談用資料ダウンロード」と「店舗POPダウンロード」の2つにする。パワーポイントのテンプレートは、「/Volumes/kh_aigrd_databricks_prd_01/default/immune_promo_nursery/pptx_template/」にto_shoudan.pptxとto_pop.pptxとして格納予定。databricksにアクセスできないときのテスト用としては、images/template.pptxを利用してください。
  

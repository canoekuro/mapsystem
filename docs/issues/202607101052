- 地図のデフォルトをgsi_paleに変更
- 詳細度ズームは最大で14。15以上は設定不可としたうえで、後は0＝固定しない（ズームに追従）の挙動をするようにする。
- 取得半径の小数点以下は不要。整数のみを入力できるようにする。最低値は1km、最大値は設定しない。
- 推進園を示す円のサイズを調整したい。設定ページで設定できるようにする。
- 店舗を示すカートのマークのサイズも設定ページで設定できるようにする。

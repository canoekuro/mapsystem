# result: 列名変更対応・出荷実績の表示（issue 202607231113）

計画（[plan](20260723-030008-issue-202607231113-plan.md)）どおり実装した。

## 変更内容

- `config/column_mapping.toml`
  - `"店舗コード" = "小売店CD"` に張替。`企業G名称` と 3商材×3指標の9列を恒等エントリで追加。
- `config/databricks_config.toml`
  - `[rdp]`（`update_toml_path` / `local_fallback`）を追加。
- `lib/data.py`
  - `SALES_PRODUCTS` / `SALES_METRICS` / `SALES_COLUMNS` / `sales_column()` を追加。
  - `load_stores` の SELECT に `企業G名称`＋実績9列を追加。
  - `store_nursery_counts` の出力へ実績9列を追加（`stores_df` に存在する分のみ、`推進園数` の後）。
  - `load_shipment_period()` を新設（`_load_rdp_config` / `_read_rdp_update_text` を含む）。
    Volume 取得失敗時はローカルfallback。`_PERIOD_RE` で `（開始 ～ 終了）` を解析。
- `views/main_page.py`
  - `_sales_table_html(srow, period)` へ変更、`_fmt_boxes` / `_fmt_yoy` を追加。
  - 呼び出しを `load_shipment_period()` 併用に更新、`load_shipment_period` を import。
- `scripts/gen_sample.py`：`企業G名称`・実績9列を店舗単位で生成。`data/master.csv` を再生成（208行）。
- `SPEC.md`：§4.2 カラム定義（改名・追加列・期間ファイル）、§6.1.2 出荷実績テーブルを追記。

## 検証結果

- `import lib.data, views.main_page`：成功。
- `load_shipment_period()`：Volume 未接続環境でローカルfallback（`docs/issues/rdp_update.toml`）
  から `2026年06月～2026年06月` を取得。
- `store_nursery_counts`：実績9列付きで返却。実績列を持たない DataFrame では従来どおり
  `店舗名称/店舗コード/店舗_都道府県/推進園数` の4列で返り、壊れない。
- `_sales_table_html`：実績（箱数）=`100.0` をそのまま、前年比 `1.111`→`111.1%`、
  欠損→`—`、`period=None`→`出荷実績　期間：—` を確認。
- `scripts/gen_sample.py` 実行で新スキーマの `data/master.csv` を生成。

## 未対応 / 補足

- 前年比の単位は「比率（例 1.053）」とユーザー確認済み。DB が別単位の場合は `_fmt_yoy` を調整。
- `企業G名称` はデータに取り込むが画面表示は未使用（将来利用余地のため mapping/取得に含めた）。
- 実 Databricks 接続での目視確認（マップ画面の出荷実績表・店舗別表・CSV）は接続環境で実施のこと。

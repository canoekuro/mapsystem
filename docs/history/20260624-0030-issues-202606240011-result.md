# 課題対応 結果（docs/issues/202606240011.md）

- 日時: 2026-06-24 00:30 着手 / 同日完了
- テーマ: 運用課題4件の修正
- 対応する計画: [20260624-0030-issues-202606240011-plan.md](20260624-0030-issues-202606240011-plan.md)

## 変更内容

- 課題1（半径円が表示されない）: `lib/data.py: zoom_for_radius` を動的算出に変更
  （`z = floor(log2(0.8*viewport*156543.03392*cos(lat)/(2*radius_km*1000)))`、0〜19 クランプ）。
  `lib/map_builder.build_map`（viewport=560）と `lib/static_map.render_static_map`（viewport=size）
  が新ズームを使用。
- 課題2（単一CSV化）: `lib/data.py: filter_company(df, company, radius_km)` を追加し、
  SC-02a で `to_csv(index=False).encode("cp932", errors="replace")` の単一CSVを出力。
- 課題3（一括分割）: `views/bulk_data.py`（SC-02a データ抽出）と `views/bulk_image.py`
  （SC-02b 画像抽出）に分割。旧 `pages/map_bulk.py` を廃止。`lib/zip_builder.build_csv_zip` 削除。
- 課題4（自動ナビ除去）: `pages/` を `views/` に改名し Streamlit 自動マルチページを回避。
  `app.py` の radio を4項目化し `views.*` をディスパッチ。
- SPEC.md（§5.1/§6.1.2/§6.2/§7/§9/§12）、README.md、CHANGELOG.md を新仕様へ更新。

## 検証結果

- `zoom_for_radius` 検算: radius 0.1/0.5/1/2/5/10/20/50 のいずれも円直径が 656px 以内に収まる
  （旧段階式では radius 2km が画面外だった）。
- `render_static_map`（radius 2km/5km）を生成・目視: 紫の半径円が画像内に完全収容、
  店舗マーカー・番号付き色分けマーカー・凡例も表示。
- `filter_company` → cp932 CSV のエンコード/デコード成功（7,680 bytes）。
- `build_png_zip` の画像ZIP生成は従来どおり動作。
- `streamlit run app.py`: 起動 OK（health 200・エラーなし）。`pages/` 廃止により自動ページ
  一覧は表示されず、ラジオ4項目で切替。
- Databricks 再デプロイ: ワークスペース上の旧 `pages/` を `workspace delete --recursive` で
  除去 → `--full` 同期 → `apps deploy`。state=RUNNING、URL
  `https://mapsystem-130773044949419.aws.databricksapps.com`。

## 未対応事項・留意

- SC-01（店舗単位）の CSV は課題対象外のため UTF-8 のまま（cp932 化の要望があれば対応）。
- cp932 エンコード不能文字は `errors="replace"` で代替（Excel 互換優先）。
- Databricks 上での円表示・CSV(cp932)・画像ZIP の最終目視はユーザー確認（PAT ではログ取得不可）。
- 再デプロイ時はワークスペースの旧 `pages/` ディレクトリ残存に注意（今回 recursive 削除で対応）。

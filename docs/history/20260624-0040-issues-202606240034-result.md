# 単一ページ再設計 結果（docs/issues/202606240034.md）

- 日時: 2026-06-24 00:40 着手 / 同日完了
- 対応する計画: [20260624-0040-issues-202606240034-plan.md](20260624-0040-issues-202606240034-plan.md)

## 変更内容

- `lib/data.py`: `stores_for_company(df, company)` を追加（企業→小売店のカスケード絞込）。
- `views/main_page.py`（新規）: 単一ページの `render(df)`。上部に企業/小売店/半径の選択
  （`st.columns(3)`、企業選択で小売店をカスケード）、その下に企業一括データ（cp932 CSV を
  `st.download_button` 直接）と企業一括画像（生成ボタン→`st.progress`→`st.download_button` の2段）。
  店舗選択時に紫ヘッダー＋件数 metric＋`st.columns([2,1])` の地図（`st_folium`）＋施設リスト。
  最下部に `st.caption` で出典を小さく表示。
- `app.py`: サイドバー/ラジオを撤去し、`load_master`→`main_page.render(df)` のみ。
- 旧 view（`views/map_single.py`/`bulk_data.py`/`bulk_image.py`/`help.py`）を削除。
- SPEC.md（§5/§6/§7/§9/§12）・README.md・CHANGELOG.md を単一ページ構成へ更新。
  店舗単位DL（旧 F-02/F-03）は廃止として明記。

## 検証結果

- `streamlit run app.py`: health 200・エラーなし。ヘッドレス Chrome のスクリーンショットで
  単一ページ（サイドバー/ラジオ無し、上部に3選択＋2ボタン、未選択時の案内、出典フッタ）を
  目視確認。
- `stores_for_company` 動作、cp932 CSV 生成は既存検証済みパスを再利用。
- Databricks 再デプロイ: ワークスペースの旧 view 4ファイルを `workspace delete` で除去し、
  `apps deploy` → state=RUNNING。URL `https://mapsystem-130773044949419.aws.databricksapps.com`。

## 未対応事項・留意

- 企業一括画像は店舗数×タイル取得で重く、1リクエスト120秒上限を超え得る（小〜中規模対象）。
- `databricks sync --full` はローカル削除ファイルを workspace から自動削除しないため、旧 view は
  `workspace delete` で明示除去した（今後の再編時も同様に注意）。
- Databricks 上での操作（カスケード・地図・CSV/画像DL）の最終目視はユーザー確認。

# 結果: 推進園区分による地図の色分け実装

- **日時:** 2026-07-07 09:06
- **対応 plan:** `docs/history/20260707-0906-suisinen-category-color-plan.md`

## 1. 変更内容

計画どおり、色分けキーを実値（`認可保育所` / `認定こども園` / `幼稚園`）へ更新した。

| ファイル | 変更 |
|---|---|
| `lib/map_builder.py` | `_FACILITY_COLORS` のキーを実値化 |
| `lib/png_builder.py` | 同上 |
| `lib/static_map.py` | 同上（RGBタプル） |
| `views/main_page.py` | 施設リストバッジの `_FACILITY_COLORS` を実値化 |
| `scripts/gen_sample.py` | 表示名末尾語と `推進園区分` を分離。名称は `〜保育園` 等の自然名を維持し、区分は `認可保育所` / `認定こども園` / `幼稚園` にマッピング |
| `data/master.csv` | 再生成（195行） |
| `SPEC.md` | §4.2 の `推進園区分` を必須・3区分・色分け使用に更新。§6.1.2 の色分け表を実値へ更新 |

色の割り当ては既存の3色を踏襲（緑=認可保育所、黄=認定こども園、赤=幼稚園）。凡例は追加していない。

## 2. 検証結果

- `python3 scripts/gen_sample.py` → `生成完了: 195行`。
- 再生成後の `推進園区分` 集計: `{'認可保育所': 96, '幼稚園': 52, '認定こども園': 47}`（旧値なし）。
- 施設名サンプル: `あおぞら保育園 / 認可保育所`、`さくら幼稚園 / 幼稚園`、`ちどりこども園 / 認定こども園` — 名称は自然、区分は実値。
- AST 検証: `lib/map_builder.py`（注釈付き代入）/ `lib/png_builder.py` / `lib/static_map.py` / `views/main_page.py` の `_FACILITY_COLORS` がいずれも3実値をキーに持ち、フォールバックに落ちないことを確認。
- 依存（streamlit 等）が本環境に無いためアプリの実起動（Streamlit/Databricks/selenium）は未実施。色分けは `dict.get(区分, フォールバック)` のキー一致のみに依存する純粋なデータ変更であり、キー網羅を静的・データ両面で確認した。

## 3. 未対応事項

- `_FACILITY_COLORS` の4ファイル重複は未集約（今回のドリフトの根本原因）。共有定数への集約は
  フォローアップ課題として残す。
- 実アプリ（Databricks Apps）上での目視確認は本環境では不可。
